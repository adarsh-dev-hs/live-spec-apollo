---
title: "Part 3 — Quarter 3"
description: "Full depth: payroll, analytics and MIS, the remaining intake channels, workforce operations, the CPAL migration, rate variance, collections and go-live."
icon: Target
---

**Due: end of Quarter 3.** Full depth.

Parts 1 and 2 made every thread work. Part 3 takes each of them to its full width, moves Apollo's
existing data onto the platform, and turns the platform into the system of record.

## 1. What becomes available

As the SOW states it.

| Capability | What Part 3 carries |
|---|---|
| **Payroll** | All live. **Work starts in Q1.** |
| **Analytics and MIS** | Analytics and management information. |
| **Multi-channel intake** | The remaining intake channels. |
| **Workforce operations** | Workforce operations live. |
| **Legacy migration** | CIEPAL. |
| **Rate variance management** | Staffing. |

**Payroll work starts in Quarter 1 even though payroll goes live in Quarter 3.** It integrates a
third-party engine whose selection, contract and field mapping are all client dependencies, and it
sits behind associate onboarding, attendance and workforce operations. Starting it in Quarter 3
would put it behind three predecessors with no room left.

## 2. Deliverables in Part 3

`D3` (partial) · `D4` (partial) · `D5` (partial) · `D9` · `D11` (partial) · `D13` (partial) ·
`D14` (partial) · `D16` (partial) · `D18` (partial) · `D20` (partial) · `D21` (partial) ·
`D22` (partial) · `D23` · `D24` · `D25` (partial) · `D28` · `D30` · `A1` (partial) · `A6` (partial) ·
`A8` · `A10` · `A11` · `A14` (partial)

**Twenty-three deliverable references.** `D9`, `D23`, `D24`, `D28`, `D30`, `A8`, `A10` and `A11` are
delivered entirely here.

### The slice each deliverable contributes

| Ref | Deliverable | The Part 3 slice |
|---|---|---|
| `A1` | Multi-channel demand ingestion | The remaining channels: per-client spreadsheet formats, client HRMS and ATS feeds, and WhatsApp voice notes transcribed to the requisition schema. |
| `D14` | Prioritisation and allocation **(full)** | The prioritisation engine, suggested recruiter allocation with load balancing, and the requisition approval workflow. **The system suggests; a person still decides.** |
| `D9` | Intelligence and analytics | Funnel and conversion, fill rate, time-to-fill, trial survival. |
| `D28` | Analytics — performance and MIS | Stage-level conversion loss by client and recruiter, channel and recruiter performance, and the monthly MIS pack. |
| `A11` | Rate variance instrumentation | Rate agreed against baseline, by client, role, specialty and acuity. |
| `A8` | Outcome write-back to ranking | Selection outcomes and rejection reasons feeding subsequent ranking. |
| `D22` | Time and attendance **(full)** | Leave categorisation, overtime treatment, and internal timesheet approval routing. |
| `D23` | Workforce operations | The maker-checker request engine, attendance upload paired against app check-ins, salary revision individual and bulk, advance, hold and release. |
| `D24` | Payroll | Third-party engine integration, payroll inputs and cycle configuration, PF and ESIC reports and challans, payslips in-app and over WhatsApp. |
| `D25` | Billing — remainder and collections **(full)** | Payment receipt, reconciliation including part-payments and TDS, ageing, reminders and dunning, credit and debit notes. |
| `D21` | Associate onboarding | The document checklist and the app-guided onboarding flow, plus the gate that blocks a pay run for incomplete paperwork. |
| `A10` | Legacy migration (CPAL) | Profiling, the pipeline, duplicate resolution against the app-registered base, reconciliation, and the cutover that makes the platform system of record. |
| `D3` | Candidate mobile app | Resume parsing at width, payslips in the app. |
| `D4` `D16` | Verification | Document OCR at width, and credential expiry tracking across the whole base. |
| `D5` `D18` | Matching and shortlisting | Weight tuning per category and base, informed by the `A8` outcome data. |
| `D20` | Offer management | Digital signature on the offer. |
| `A6` | Trial period tracking | The trial dashboard at width and escalation on negative responses. |
| `D11` `D30` | Foundations and documentation | Technical and user documentation, UAT support, cutover and go-live for everything released after the first go-live. |

## 3. The migration is the part with the least known about it

`A10` moves Apollo's candidate data out of CPAL, after which the old tool is switched off. **Its
effort basis awaits joint data profiling**, which has not happened: nobody has seen the volume, the
completeness, the duplicate rate, or whether fields map cleanly.

Profiling is the first piece of work on it, and it belongs early — nothing else about `A10` can be
sized until it has run, and its result affects the Part 3 plan more than any other single input.

## 4. Screens Part 3 adds

Reporting — `/reports/funnel`, `/reports/fill`, `/reports/trial-survival`, `/reports/stage-loss`,
`/reports/performance`, `/reports/rate-variance`, `/reports/monthly-pack`.
Workforce and payroll — `/workforce/requests`, `/workforce/payroll`, `/app/payslips`.
Administration at width — `/admin/ranking-weights`, `/admin/registries`, `/admin/link-types`,
`/admin/jobs`, `/admin/system`.

## 5. Part 3 is done when

1. A requisition arrives on all five live channels plus verbal capture, and the six produce the same
   record.
2. Every report in `D9` and `D28` covers the whole period since the first requisition — not since
   the reports were built.
3. A pay run completes end to end through the third-party engine, with PF and ESIC reports produced
   and payslips delivered in the app and over WhatsApp.
4. A salary revision needs two people, and neither of them can be the same person.
5. A payment is received, matched across invoices with part-payment and TDS handled, and ageing runs
   off the contract payment terms rather than a flat rule.
6. The migrated base reconciles against the source total, duplicates against app registrations are
   resolved, and CPAL is switched off.
7. Documentation, UAT and cutover are complete for everything released after the first go-live.
